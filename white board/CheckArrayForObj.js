var array = [1, 2, 3];

function greet(param) {
    if (Object.prototype.toString.call(arrayList) === '[abject Array]') {
        console.log('Array');
    } else {
        console.log("Not an array");
    }
}

function greet2(param) {
    if ($.isArray(arrayList)) {
        console.log('Array');
    } else {
        console.log('Not an array');
    }
}